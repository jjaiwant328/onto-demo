# ---- Node / AppKit (new deployed app) ----
node_modules/
# dist/ is NOT ignored — Databricks Apps platform needs pre-built assets
# The CI/CD pipeline runs `npm run build` before `bundle deploy`
build/
.databricks/
.smoke-test/
test-results/
playwright-report/
# Auto-generated AppKit types (endpoint-specific, varies per developer)
shared/appkit-types/serving.d.ts

# ---- Python (legacy_streamlit_app) ----
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ipynb_checkpoints/
# Generated synthetic seed (recreate with: python legacy_streamlit_app/app/seed/generate_seed.py)
legacy_streamlit_app/app/seed/*.parquet

# ---- Local env ----
.env
*.local
.DS_Store

# ---- Bundled demo schemas & scratch inputs (kept locally, never committed) ----
client/src/data/schema.qsr.json
client/src/data/schema.qsr_sc.json
inputs/qsr_full_with_noise.csv
inputs/retailer_dims_bi.csv
inputs/retailer_dims_pos.csv
