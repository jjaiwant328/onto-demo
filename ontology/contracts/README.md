# ontology/contracts

The **canonical** data contract is governed by Ontos and lives at:

    ontos/contracts/jai_store_traffic_labor_efficiency_contract.yaml

This folder is intentionally a pointer to avoid contract drift between the
ontology design layer (OntoBricks) and the governance layer (Ontos). Edit the
Ontos copy; do not fork it here.
