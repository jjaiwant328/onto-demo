streamlit>=1.33
pandas>=2.0
pyarrow>=14.0
numpy>=1.24
PyYAML>=6.0
databricks-sql-connector>=3.0   # warehouse mode with a PAT (DATABRICKS_TOKEN)
databricks-sdk>=0.30            # warehouse mode with OAuth/CLI profile (no PAT)
